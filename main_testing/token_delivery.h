// Global servo objects to control the token arms
// note: connect the Arduino gnd to all servo gnds

#ifndef __TOKEN_DELIVERY_H__
#define __TOKEN_DELIVERY_H__

#include <Arduino.h>
#include "pin_mapping.h"

bool tokenTestLoop(char userInput);

#endif //__TOKEN_DELIVERY_H__
